[build]
  base = "apps/web"
  command = "npm install --legacy-peer-deps && NODE_ENV=production npm run build"
  publish = "build/client"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "build/server"
  node_bundler = "esbuild"

